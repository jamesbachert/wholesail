# Project Instructions

## End-of-Session Routine
When the user says they're done for the day (e.g., "I'm going to bed", "done for the day", "signing off", "calling it a night"):
1. Merge any worktree branches into `main`
2. Push everything to GitHub remote so it's backed up
These should be the **last steps** of every session.
